//! `ttd`: the vprog-tictactoe node daemon.
//!
//! Drives the vprogs runner with the tic-tac-toe guest program and serves the DA
//! (frontend-rendering data) API. Skeleton only; the runner wiring and DA server land with the
//! node milestone.

fn main() {
    println!("ttd: node daemon skeleton");
}
